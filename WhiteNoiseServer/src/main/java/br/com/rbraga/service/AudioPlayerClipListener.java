package br.com.rbraga.service;

import javax.sound.sampled.LineEvent;
import javax.sound.sampled.LineListener;

public class AudioPlayerClipListener implements LineListener {
//	private final Logger log = LoggerFactory.getLogger(AudioPlayerClipListener.class);

	@Override
	public void update(LineEvent event) {
	}

}
